/* Factory that returns functions that parse data when a user has logged in with a specific provider */
app.factory("UserFactory", function($firebaseObject, $firebaseArray){
  var parseEmailData = (authData, personalData) => {
    var authDataPath = authData.password;
    return {
      data
    }
  }

  return {

  }
})
